<CdrDoc Type='Filter' Id='CDR0000000084'>
<CdrDocCtl>
<DocValStatus readonly="yes">U</DocValStatus>
<DocActiveStatus readonly="yes">A</DocActiveStatus>
<DocTitle readonly="yes">Organization Address Fragment</DocTitle>
</CdrDocCtl>
<CdrDocXml><![CDATA[<?xml version="1.0"?>

<xsl:transform       xmlns:xsl = "http://www.w3.org/1999/XSL/Transform"
                     xmlns:cdr = "cips.nci.nih.gov/cdr"
                       version = "1.0">

 <!-- Passed in by the caller -->
 <xsl:param               name = "fragId"/>

 <!-- Start at the top.  We only want Location elements. -->
 <xsl:template           match = "/">
  <AddressElements>
   <xsl:apply-templates select = 
"Organization/OrganizationLocations/OrganizationLocation/Location[@cdr:id = $fragId]"/>
  </AddressElements>
 </xsl:template>

 <!-- Identity rule, copies everything to the output tree. -->
 <xsl:template           match = "@*|node()">
  <xsl:copy>
   <xsl:apply-templates select = "@*|node()"/>
  </xsl:copy>
 </xsl:template>

 <!-- Extract the address information for the Location element -->
 <xsl:template           match = "Location">
   <xsl:apply-templates  select = "node()"/>
 </xsl:template>

</xsl:transform>
]]>
</CdrDocXml>
</CdrDoc>